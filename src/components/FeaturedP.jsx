import React from "react";

const FeaturedP = ({ text }) => {
  return (
    <>
      <p className="text-lg font-secondary text-themeBlue lg:text-base">{text}</p>
    </>
  );
};

export default FeaturedP;
